# contracts/ — 001-add-nextjs-app

This feature (initial Next.js scaffold) does not introduce any external HTTP APIs or contracts.

If future features add backend endpoints, add OpenAPI/GraphQL contracts here using standard patterns (OpenAPI v3 YAML/JSON or GraphQL schema files).
