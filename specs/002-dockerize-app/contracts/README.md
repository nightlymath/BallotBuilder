# contracts/

This feature does not change the public API surface of the application.

- No REST or GraphQL contracts added or modified by `002-dockerize-app`.
- Contracts directory is present for completeness and future contract-first work.

If future iterations add API or schema changes, add OpenAPI/GraphQL schemas into this directory and include contract tests in CI.
